{
  "id": "azula",
  "name": "azula",
  "version": "0.0.1",
  "description": "Reach your OpenClaw bot from your phone over azula's end-to-end iroh transport, instead of through a third-party messenger.",
  "channelConfigs": {
    "azula": {
      "schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "type": "boolean",
            "default": true,
            "description": "Whether this azula channel is active."
          },
          "device": {
            "type": "string",
            "minLength": 1,
            "description": "The azula device to talk to, by the name it has in `azula devices`. Must already be paired with this machine."
          },
          "binary": {
            "type": "string",
            "minLength": 1,
            "default": "azula",
            "description": "Path to the azula binary. Leave unset to use `azula` from PATH."
          },
          "session": {
            "type": "string",
            "minLength": 1,
            "default": "openclaw",
            "description": "azula session name. Persistent, so the conversation on the phone survives gateway restarts rather than appearing anew each time. Give each account its own name — two accounts sharing one would fight over the same session key."
          },
          "label": {
            "type": "string",
            "minLength": 1,
            "default": "OpenClaw",
            "description": "Conversation title shown on the phone."
          },
          "dmPolicy": {
            "type": "string",
            "enum": ["allowlist", "pairing", "open"],
            "default": "allowlist",
            "description": "Reserved for parity with other channels. azula's own device pairing is the real access boundary: only a paired device can reach this machine at all."
          },
          "accounts": {
            "type": "object",
            "description": "Additional named accounts, each addressing its own device. Fields omitted here inherit from the root.",
            "additionalProperties": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "enabled": { "type": "boolean" },
                "device": { "type": "string", "minLength": 1 },
                "binary": { "type": "string", "minLength": 1 },
                "session": { "type": "string", "minLength": 1 },
                "label": { "type": "string", "minLength": 1 }
              }
            }
          }
        },
        "required": ["device"]
      }
    }
  }
}
